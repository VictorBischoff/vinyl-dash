If you want, next step we can:
	•	Add BPM range filters (e.g., “show all records between 85–100 BPM”)
	•	Add a toggle to view everything as a table instead of cards
	•	Improve the lookup so it uses track titles from Discogs instead of release titles for more accurate BPMs.